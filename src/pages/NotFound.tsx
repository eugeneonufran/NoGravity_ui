export const NotFound = () => {
  const styles = {
    color: "red",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  };

  return (
    <div style={styles}>
      <h1>NotFound</h1>
    </div>
  );
};
